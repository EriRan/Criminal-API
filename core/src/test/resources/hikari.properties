#Difference here compared to the properties in main.java is that we point to the database container
#and not through the docker namespace
jdbcUrl=jdbc:postgresql://localhost:5432/postgres
dataSource.user=root
dataSource.password=testdb