type Query {
  criminalById(id: ID!): Criminal
}

type Criminal {
  id: ID!
  name: String!
  appearance: String!
  charges: [Charge]
}

type Charge {
  id: ID!
  description: String!
}

schema {
  query: Query
}